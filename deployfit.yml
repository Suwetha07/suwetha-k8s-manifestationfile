apiVersion: apps/v1
kind: Deployment
metadata:
  name: fit-deployment
  namespace: suwetha-ns
  labels:
    app: fit-app
spec:
  replicas: 8
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector: 
    matchLabels:
      app: fit
  template:
    metadata:
      labels:
        app: fit
    spec:
      containers:
        - name: fit-conatiner
          image: muralisocial123/fitness-app:v1.1.0
          ports:
            - containerPort: 3000